Gravity = require './gravity'
Board = require './board'
Printer = require './printer'

module.exports = class RandomArt
  @get: (data, opt) ->

  @toRawBytes: (str, fmt) ->

  @discoverFormat: (str) ->
